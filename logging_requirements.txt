# AgentBus日志和监控系统依赖

# 核心依赖
psutil>=5.8.0          # 系统和进程监控

# 可选依赖（用于高级功能）
requests>=2.25.0       # HTTP请求（Webhook通知）
# email模块是Python标准库的一部分，无需单独安装

# 开发依赖
pytest>=6.0.0         # 测试框架
pytest-cov>=2.10.0    # 测试覆盖率

# 文档依赖
sphinx>=4.0.0         # 文档生成